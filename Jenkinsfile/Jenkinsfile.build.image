pipeline {
  stages {
    stage('build docker image'){
        when {
          branch 'master'
        }
        steps{
          acrQuickTask azureCredentialsId: 'my service principal', gitPath: '', gitRefspec: '', gitRepo: '', imageNames: [[image: 'ubuntu:v2.0']], local: '/home/azureuser/docker', registryName: 'feiacr', resourceGroupName: 'andy-cf-concourse', tarball: '', variant: ''
        }
    }
  }
}
